# 项目完整理解 - 给技术小白的说明

## 一、您现在有什么？

### 现有系统：Anx Showtime（工时智能工作台）

**这是什么？**
一个已经在运行的网站系统，用来统计和分析员工工时。

**它能做什么？**
1. 员工上传工时 Excel 文件
2. 系统自动分析工时数据
3. 生成各种统计图表和报表
4. 查看项目进度、工时分布、审批情况等

**技术架构（简单说）：**
```
前端（网页界面）
    ↓
后端（处理数据）
    ↓
SQLite 数据库（存储数据，就是一个文件）
```

**部署在哪里？**
- Hugging Face Spaces（一个云平台）
- 通过浏览器就能访问

**数据来源：**
- 目前是手动上传 Excel 文件
- Excel 文件是从飞书导出的

---

## 二、现在的问题是什么？

### 当前流程的痛点

**现状：**
```
员工在飞书填写工时
    ↓
手动导出 Excel
    ↓
手动上传到网站
    ↓
网站显示统计
```

**问题：**
1. ❌ 需要手动导出 Excel（麻烦）
2. ❌ 需要手动上传（容易忘记）
3. ❌ 数据不是实时的（有延迟）
4. ❌ 审批流程在飞书里，网站看不到
5. ❌ 容易出错（人工操作）

---

## 三、您想要做什么？

### 新系统：飞书工时管理自动化系统

**目标：**
让飞书和网站自动连接，不需要人工干预。

**理想流程：**
```
员工在飞书填写工时
    ↓
飞书自动审批（部门负责人 → 项目经理）
    ↓
【新建的中间服务】自动同步
    ↓
网站自动更新显示
```

**核心改进：**
1. ✅ 全自动，不需要手动操作
2. ✅ 实时同步（每周自动）
3. ✅ 审批流程自动化
4. ✅ 数据准确，减少人为错误

---

## 四、新系统的架构

### 三层结构

```
┌─────────────────────────────────────┐
│  第一层：飞书平台                      │
│  - 员工填写工时                        │
│  - 部门负责人审批                      │
│  - 项目经理审批                        │
│  - 数据存在飞书多维表格里              │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  第二层：中间服务（新建）              │
│  - 从飞书读取工时数据                  │
│  - 处理审批流程                        │
│  - 验证数据                           │
│  - 每周自动同步                        │
│  - 数据存在 SQLite 数据库              │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│  第三层：现有网站（已有）              │
│  - 接收同步的数据                      │
│  - 显示统计图表                        │
│  - 数据存在 SQLite 数据库              │
└─────────────────────────────────────┘
```

### 用生活例子理解

**飞书** = 公司的考勤打卡机
- 员工每天打卡记录工时
- 领导审批确认

**中间服务** = 自动传送带
- 每周自动把打卡记录送到财务部
- 检查数据是否正确
- 记录传送日志

**现有网站** = 财务部的统计系统
- 收到数据后自动生成报表
- 显示各种统计图表

---

## 五、为什么需要中间服务？

### 不能直接连接吗？

**为什么不能：飞书 → 网站？**

因为需要做很多事情：

1. **审批流程管理**
   - 部门负责人审批
   - 项目经理审批
   - 审批通知
   - 审批记录

2. **数据验证**
   - 工时是否合理（0-8小时）
   - 日期是否正确
   - 项目名称是否存在
   - 必填字段是否完整

3. **业务规则**
   - 假期处理
   - 标准工时计算
   - 非项目工时分类
   - 审批路由（找到正确的审批人）

4. **定时同步**
   - 每周一自动执行
   - 失败自动重试
   - 记录同步日志
   - 发送告警通知

**所以需要一个"中间人"来处理这些复杂的事情。**

---

## 六、关于数据库的问题

### PostgreSQL vs SQLite

**您的疑问：**
> 为什么需求文档要求 PostgreSQL？
> 我现在用的是 SQLite，会不会冲突？

**答案：**

#### 现状
- 您的现有网站用 SQLite ✅
- 运行良好 ✅
- 不需要改变 ✅

#### 新系统
- 中间服务也可以用 SQLite ✅
- 开发阶段完全够用 ✅
- 不会冲突 ✅

#### 为什么需求文档提到 PostgreSQL？

**需求文档是为"最终生产环境"写的。**

就像：
- 学开车时用教练车（SQLite）
- 拿到驾照后买自己的车（PostgreSQL）

**对比：**

| 特性 | SQLite | PostgreSQL |
|------|--------|------------|
| 安装 | 不需要（Python内置） | 需要安装服务 |
| 适合场景 | 开发、小规模 | 生产、大规模 |
| 并发能力 | 低（单文件） | 高（专业数据库） |
| 数据量 | 小（几百MB） | 大（几GB到TB） |
| 学习成本 | 低 | 中 |

**我们的决定：**
- ✅ 开发阶段用 SQLite
- ✅ 将来需要时再升级到 PostgreSQL
- ✅ 代码已经支持两种数据库，切换很简单

---

## 七、Redis 是什么？需要吗？

### Redis 的作用

**Redis = 超快的便签本**

**用途：**
1. 缓存飞书 API 的访问令牌
2. 缓存常用查询结果
3. 加快系统速度

**需要吗？**

**开发阶段：不需要 ❌**
- 系统会慢一点点
- 但功能完全正常
- 可以后期再加

**生产环境：建议有 ✅**
- 提高性能
- 减少 API 调用
- 但不是必须的

---

## 八、您需要做什么？

### 开发任务概览

根据任务清单（tasks.md），一共 39 个任务，分 7 个阶段：

#### 阶段 1：环境准备（已完成 ✅）
- ✅ Python 已安装
- ✅ Git 已安装
- ✅ SQLite 内置
- ✅ 开发环境就绪

#### 阶段 2：飞书配置（5个任务）
创建飞书多维表格：
1. 工时填报表
2. 项目配置表
3. 标准工时配置表
4. 假期日历表
5. 配置飞书应用权限

#### 阶段 3：中间服务开发（10个任务）
编写 Python 代码：
1. 数据模型
2. 飞书 API 客户端
3. 数据验证服务
4. 审批路由服务
5. 审批处理服务
6. 数据同步服务
7. 定时任务调度器
8. 数据库管理
9. 错误处理和日志
10. 网站 API 客户端

#### 阶段 4：审批流程（5个任务）
实现审批逻辑：
1. 工时提交流程
2. 部门负责人审批
3. 项目经理审批
4. 审批超时提醒
5. 工时修改和撤回

#### 阶段 5：数据同步（4个任务）
实现自动同步：
1. 周期同步任务
2. 手动同步功能
3. 同步重试和错误处理
4. 数据加密传输

#### 阶段 6：测试（3个任务）
确保质量：
1. 单元测试
2. 端到端测试
3. 性能测试

#### 阶段 7：部署上线（5个任务）
发布系统：
1. 生产环境准备
2. 应用部署
3. 数据迁移
4. 上线前检查
5. 正式上线

---

## 九、技术术语解释

### 给技术小白的词汇表

| 术语 | 简单解释 | 生活例子 |
|------|---------|---------|
| **API** | 系统间的"对话通道" | 两个人打电话 |
| **数据库** | 存储数据的"仓库" | 图书馆 |
| **SQLite** | 轻量级数据库（一个文件） | 个人笔记本 |
| **PostgreSQL** | 专业数据库（需要安装） | 公司档案室 |
| **Redis** | 超快的缓存（临时记忆） | 便签纸 |
| **FastAPI** | Python 网站框架 | 建房子的工具箱 |
| **中间服务** | 连接两个系统的"桥梁" | 翻译官 |
| **审批流程** | 工时需要经过的审批步骤 | 请假需要领导签字 |
| **同步** | 把数据从一个地方复制到另一个地方 | 把文件从U盘拷到电脑 |
| **定时任务** | 定时自动执行的操作 | 闹钟 |

---

## 十、常见问题解答

### Q1: 会不会影响现有系统？

**答：不会。**

- 现有网站继续正常运行 ✅
- 新系统是独立的 ✅
- 两个系统通过 API 连接 ✅
- 互不干扰 ✅

### Q2: 数据会不会冲突？

**答：不会。**

- 现有网站：`timesheet.db`
- 新中间服务：`feishu_timesheet.db`
- 两个独立的数据库文件 ✅

### Q3: 需要学习很多新技术吗？

**答：不需要。**

- Python 您已经会了 ✅
- FastAPI 和现有后端一样 ✅
- SQLite 和现有系统一样 ✅
- 主要是业务逻辑，不是新技术 ✅

### Q4: 开发周期多长？

**答：根据任务清单估算约 2.5 个月。**

但可以分阶段：
- 第一阶段：基础功能（1个月）
- 第二阶段：完善功能（1个月）
- 第三阶段：测试上线（0.5个月）

### Q5: 能不能先做一个简化版？

**答：可以！**

最小可用版本（MVP）：
1. ✅ 飞书读取工时数据
2. ✅ 简单的审批流程
3. ✅ 手动同步到网站
4. ❌ 暂不做自动定时同步
5. ❌ 暂不做复杂的错误处理

**先跑起来，再慢慢完善。**

---

## 十一、下一步建议

### 立即可以做的

1. **理解业务流程**
   - 看看飞书里员工怎么填写工时
   - 了解审批流程是怎样的
   - 看看现有网站的数据格式

2. **准备飞书环境**
   - 在飞书创建测试用的多维表格
   - 申请飞书应用权限
   - 获取 App ID 和 App Secret

3. **搭建开发环境**
   - 创建新的 Python 项目
   - 安装必要的依赖包
   - 配置开发工具

### 循序渐进的路线

**第一步：读取数据**
- 从飞书读取工时数据
- 打印到控制台
- 验证数据格式正确

**第二步：存储数据**
- 把数据存到 SQLite
- 查询验证数据
- 确保数据完整

**第三步：同步数据**
- 把数据发送到现有网站
- 验证网站能收到
- 检查数据一致性

**第四步：加入审批**
- 实现简单的审批逻辑
- 测试审批流程
- 完善审批功能

**第五步：自动化**
- 添加定时任务
- 实现自动同步
- 完善错误处理

---

## 十二、总结

### 核心要点

1. **现有系统**
   - 已经有一个工时统计网站 ✅
   - 运行良好，不需要改动 ✅
   - 用 SQLite 数据库 ✅

2. **新系统目标**
   - 自动化飞书和网站的连接 🎯
   - 不需要手动导出上传 🎯
   - 实现审批流程自动化 🎯

3. **技术选择**
   - 开发阶段用 SQLite ✅
   - 不需要安装 PostgreSQL ✅
   - 不需要安装 Redis ✅
   - 用熟悉的 Python + FastAPI ✅

4. **开发策略**
   - 分阶段实施 ✅
   - 先做最小可用版本 ✅
   - 逐步完善功能 ✅
   - 不影响现有系统 ✅

### 您现在应该明白了

- ✅ 为什么要做这个系统
- ✅ 系统的整体架构
- ✅ 各个组件的作用
- ✅ 为什么需要中间服务
- ✅ 数据库的选择
- ✅ 开发的大致步骤

### 准备好了吗？

如果您理解了上面的内容，我们可以：

1. 继续执行任务 2：项目结构初始化
2. 或者先做一些准备工作
3. 或者您还有其他问题

**请告诉我您想怎么继续！**
